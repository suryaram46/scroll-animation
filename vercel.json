{
    "buildCommand": "if [ -d \"app\" ]; then cd app && npm run build; else npm run build; fi",
    "outputDirectory": "app/dist",
    "installCommand": "if [ -d \"app\" ]; then cd app && npm install; else npm install; fi",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app/dist/index.html"
        }
    ]
}